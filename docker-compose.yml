version: "3.8"
services:
  portfolio:
    build: .
    image: portfolio:latest
    ports:
      - "8080:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "--timeout=2", "http://localhost/"]
      interval: 30s
      timeout: 3s
      retries: 3
